# --- core server ---
Flask==3.0.3
gunicorn==21.2.0
Werkzeug==3.0.2

# --- OpenAI + HTTP client ---
openai==1.23.0
httpx==0.27.0          # pinned for openai>=1.0 compatibility

# --- AWS / storage ---
boto3==1.34.112        # pulls in botocore automatically

# --- data / processing ---
pandas==2.2.2
opencv-python-headless==4.9.0.80   # for frame extraction (no GUI libs)
moviepy==1.0.3                     # for audio extraction

# --- confidence-scoring helper (external) ---
llm-confidence==0.1.0              # provides LogprobsHandler
